[plugin]

name="Export build"
description="One button in the top bar: saves open scenes, bumps the version and exports the next playtest build to ../Exports (runs tools/export_build.bat)."
author="Roboto"
version="1.0"
script="plugin.gd"
